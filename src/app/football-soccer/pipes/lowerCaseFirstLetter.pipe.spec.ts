/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { LowerCaseFirstLetterPipe } from './lowerCaseFirstLetter.pipe';

describe('Pipe: LowerCaseFirstLettere', () => {
  it('create an instance', () => {
    let pipe = new LowerCaseFirstLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
